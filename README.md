This is the **G**o **F**ile **S**erver
Its a simple hot reload server written in go.
### command line arguments:
`root`: the root directory of the build target.
`build`: the build command for building the target.
`exec`: the run command for running the target.

### build and run using
make
make run
